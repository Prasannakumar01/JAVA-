package selenium;
/*
import java.util.concurrent.TimeUnit;

import org.openqa.selenium.By;
import org.openqa.selenium.WebDriver;
import org.openqa.selenium.chrome.ChromeDriver;
import org.openqa.selenium.support.ui.ExpectedConditions;
import org.openqa.selenium.support.ui.WebDriverWait;

public class ExpllicitWait {

	public static void main(String[] args) {

		System.setProperty("webdriver.chrome.driver","C:\\Users\\kasir\\eclipse-workspace\\Selenium -Workspace\\javaprograms\\Drivers\\chromedriver.exe");
		WebDriver driver=new ChromeDriver();
		
		//we use window maximize to maximise the window
		driver.manage().window().maximize();
		
         // we use implicitly wait to wait for the webelements to be clicked
		driver.manage().timeouts().implicitlyWait(10, TimeUnit.SECONDS);

		//How to handle the Auto suggestion in selenium
		
		driver.navigate().to("https://www.facebook.com/");

		
		
		
		//we need to create webdriver class first 
		WebDriverWait wait=new WebDriverWait(driver,10);
		
				
	}

	}*/


